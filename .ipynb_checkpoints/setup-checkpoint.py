from setuptools import setup

setup(name='hydropt',
      version='0.1',
      description='HYDROPT',
      url='',
      author='',
      author_email='',
      license=',
      packages=['hydropt'],
      install_requires=[
          'numpy',
      ],
      zip_safe=False)